# Variables de entorno para nanonauta/
# Copiar este archivo a .env y completar los valores reales.
# .env NO se sube a git (ya está en .gitignore).

PG_HOST=localhost
PG_PORT=5432
PG_DB=picosatelite
PG_USER=picosatelite
PG_PASSWORD=secret
